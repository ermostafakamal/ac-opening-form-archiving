import { ProgressOverlaySectionDirective } from './progress-overlay-section.directive';

describe('ProgressOverlaySectionDirective', () => {
  it('should create an instance', () => {
    const directive = new ProgressOverlaySectionDirective();
    expect(directive).toBeTruthy();
  });
});
